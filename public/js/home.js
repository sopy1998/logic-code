
//Go to core.js to check how the function will be edited.


// all function about user sign-in, sign-out...
let user = {
    connection : function (event) {
        event.preventDefault();
        //function to execute when button is pressed
        //core.request(...)
    },
    disconnect : function (event) {
        event.preventDefault();
        //core.request(...)
    },
    updateAccount : function (event) {
        event.preventDefault();
    },
    register : function (event) {
        event.preventDefault();
    }
};

let post = {
    open : function () {
        
    },
    close : function () {
        
    }
};

let Home = {
    refresh : function () {
        
    },
    checkConnectionStatut : function () {

    }
};